<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kangli.qms.domain.notification.mapper.NotificationMapper">

    <!-- 将指定用户的全部未读通知标记为已读 -->
    <update id="markAllRead">
        UPDATE qms.notification
        SET is_read = 1, read_at = #{readAt}::timestamp, updated_at = now()
        WHERE user_id = #{userId}
          AND is_read = 0
          AND is_deleted = 0
    </update>

    <!-- 统计未读通知总数 -->
    <select id="selectUnreadCount" resultType="java.lang.Long">
        SELECT COUNT(*) FROM qms.notification
        WHERE user_id = #{userId}
          AND is_read = 0
          AND is_deleted = 0
    </select>

    <!-- 批量逻辑删除过期的已读通知（清理 window 天数前的已读通知） -->
    <update id="batchDeleteReadExpired">
        UPDATE qms.notification
        SET is_deleted = 1, updated_at = now()
        WHERE is_read = 1
          AND is_deleted = 0
          AND created_at &lt; #{cutoff}
    </update>

</mapper>
