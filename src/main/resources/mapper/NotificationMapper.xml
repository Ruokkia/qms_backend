<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kangli.qms.mapper.NotificationMapper">

    <!-- 将指定用户的全部未读通知标记为已读 -->
    <update id="markAllRead">
        UPDATE qms.notification
        SET is_read = 1, read_at = #{readAt}::timestamp, updated_at = now()
        WHERE user_id = #{userId}
          AND is_read = 0
          AND is_deleted = 0
    </update>

    <!-- 统计未读通知总数 -->
    <select id="selectUnreadCount" resultType="java.lang.Long">
        SELECT COUNT(*) FROM qms.notification
        WHERE user_id = #{userId}
          AND is_read = 0
          AND is_deleted = 0
    </select>

</mapper>
